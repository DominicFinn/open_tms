# Use package-lock files for reproducible installs
package-lock=true

# Prefer exact versions for workspace dependencies
save-exact=false

# Ensure workspaces are properly hoisted
# This helps with optional dependencies like @rollup/rollup-darwin-arm64

